<div id="title_container"></div>

<script type="text/template" id="title-template">
    <div class="expand panel">
        <center>
            <h3>{{-project.title }}</h3>
            <a href="/conceptsMap?projectId={{- project.id}}" class="{{if(page == 'concept'){ }}success {{ } }}button secondary round tiny">concepts</a>
            <a href="/knowledges?projectId={{- project.id}}" class="{{if(page == 'knowledge'){ }}success {{ } }}button secondary round tiny">knowledges</a>
            <a href="/categories?projectId={{- project.id}}" class="{{if(page == 'category'){ }}success {{ } }}button secondary round tiny">categories</a>
            <a href="/analyse?projectId={{- project.id}}" class="{{if(page == 'cklink'){ }}success {{ } }}button secondary round tiny">CKlinks</a>
            <a href="/userManager?projectId={{- project.id}}" class="{{if(page == 'members'){ }}success {{ } }}button secondary round tiny">users</a>
        </center>
    </div>
</script>


